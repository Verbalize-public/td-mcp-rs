<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Hardened-runtime baseline: no special capabilities required today.
      Kept as a file so `codesign --entitlements` has a stable anchor if
      entitlements are ever needed (e.g. JIT for wgpu shaders). -->
<plist version="1.0">
<dict/>
</plist>
